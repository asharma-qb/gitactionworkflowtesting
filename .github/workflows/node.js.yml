# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: install ci cd
        run: npm ci
      - name: Restore node_modules from cache
        uses: actions/cache@v2
        with:
          path: |
            node_modules
            */*/node_modules
          key: ${{ runner.os }}-${{ hashFiles('**/yarn.lock') }}
      - name: Install dependencies
        run: npm run build --if-present
      - name: Run test
        run: npm test
      - name: run test
        run: npm test
      - name: Create folder
        run: |
          mkdir deploy
      - name: run npm build dist command to copy files from build folder
        run: npm run build-dist
        working-directory: deploy
      - name: curl
        uses: wei/curl@v1
        with:
          args: -X POST -H 'Content-type: application/xml' 'QUICKBASE-ACTION: API_AddReplaceDBPage'  -d '<qdbapi>
<pagename>GithubActionFlowsTesting.js</pagename>
<pagetype>1</pagetype>
          <pagebody><![CDATA[
<?xml version='1.0'?>
<xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
 version="1.0">
 <xsl:template match="/">
   <html>
    <head>
    </head>
    <body>
      Hello World
    </body>
   </html>
 </xsl:template>
</xsl:stylesheet>
]]>
</pagebody>
<usertoken>b7j53i_mzif_0_z4jfim5dptx9cknc59rbf7shi4</usertoken> 
</qdbapi>' https://demo.quickbase.com/db/bsqih8822 HTTP/1.0    